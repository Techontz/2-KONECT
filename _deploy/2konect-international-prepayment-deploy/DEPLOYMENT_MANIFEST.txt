2KONECT - INTERNATIONAL PREPAYMENT
DEPLOYMENT MANIFEST
======================================================================

Generated : 2026-08-22 17:12:59
Target    : /home/konectsh/domains/api.2konect.shop
Files     : 16  (11 new, 5 modified)

Everything in files/ mirrors the Laravel root, so the tree can be
extracted straight over the application directory.

----------------------------------------------------------------------
FILE LIST
----------------------------------------------------------------------

[NEW] app/Models/CheckoutPaymentChannel.php
    why    : The customer payment channel model (Lipa Namba, Mobile Money). Separate from PaymentMethod, which is vendor payouts.
    bytes  : 2167
    sha256 : 414766993135cf0db8416ce5ef6f7c624e0b061c618333a372ed17f9c7e125d9

[NEW] app/Support/CheckoutPolicy.php
    why    : The single place the business rules live: no COD on an import, which channels are allowed, and whether delivery may be charged at checkout.
    bytes  : 5280
    sha256 : 657f82e7bb55e2b1bcef05fc222bc5f84d28cd98b9e31d00b9c7528310037653

[NEW] app/Http/Controllers/Api/Shop/CheckoutPaymentController.php
    why    : GET payment-channels (what a basket may pay with) and POST orders/{ref}/payment (customer submits a reference). Contains no route that can mark a payment verified.
    bytes  : 3933
    sha256 : 97a126e80892f6a22abc6adcaa937b0379fca236c78e607100db4429dacf191a

[NEW] app/Filament/Resources/CheckoutPaymentChannelResource.php
    why    : Admin screen: Settings -> Customer Payment. Where the Lipa Namba number, name, instructions and active toggle are edited.
    bytes  : 5360
    sha256 : ce30c65cf95fe8a1e192037dcd3ed62c95a908c3277bef74eb15e77fe2f84772

[NEW] app/Filament/Resources/CheckoutPaymentChannelResource/Pages/ListCheckoutPaymentChannels.php
    why    : Filament list page for the resource above.
    bytes  : 318
    sha256 : fafa4dcd246f8cb9312ee820872f9d1c43cb03565f400665ecc8554b8f376bd0

[NEW] app/Filament/Resources/CheckoutPaymentChannelResource/Pages/CreateCheckoutPaymentChannel.php
    why    : Filament create page for the resource above.
    bytes  : 321
    sha256 : bd1e6f1048eed6e78b322c19856fb70d6b1b89e83022a727b82dc98030b79fbd

[NEW] app/Filament/Resources/CheckoutPaymentChannelResource/Pages/EditCheckoutPaymentChannel.php
    why    : Filament edit page for the resource above.
    bytes  : 315
    sha256 : e303ff94ce884161dd3866f9d590ef97e9cd5b8f09486ff696ea6e55bf70d201

[NEW] database/migrations/2026_08_23_000010_create_checkout_payment_channels_table.php
    why    : Creates checkout_payment_channels. Runs FIRST (timestamp 000010).
    bytes  : 2731
    sha256 : da655b20f00db214ca99b715f3ff9f1ebf8c35d37dbe005fb65e35b0b83789bc

[NEW] database/migrations/2026_08_23_000020_add_payment_state_to_orders_table.php
    why    : Adds payment_status, payment_reference, payment_submitted_at, payment_verified_at, payment_verified_by, payment_note to orders. Runs SECOND (000020). Purely additive.
    bytes  : 2777
    sha256 : 0658d2c9a06e59e8d030df6ba1b59c9d28ff7f9d159e623f887cf697b8461f9d

[NEW] database/seeders/CheckoutPaymentChannelSeeder.php
    why    : Creates the Lipa Namba and Mobile Money rows INACTIVE with number = null. firstOrCreate, so re-running never overwrites a number an admin has set.
    bytes  : 2085
    sha256 : 4f64ddf3fd6337333d37a16466ea4d77f9f497996e2cbb0c9e617c9130e2546a

[MODIFIED] app/Http/Controllers/Api/Shop/OrderController.php
    why    : Enforces the rules at checkout: dynamic payment_method validation, refuses cash_on_delivery when any line is an import, records payment_status, and no longer auto-applies the delivery fee to an imported order.
    bytes  : 24087
    sha256 : 84ab86633efcfaeaeed46ea54765f89c2375affc53cc7ec3847d1babb8dae695

[MODIFIED] app/Models/Order.php
    why    : Adds the six payment_* fields to $fillable and casts the two timestamps. Without this, mass assignment would silently drop them.
    bytes  : 3416
    sha256 : 14b24a980f4a625bb993c70f89f050ef92cf9c9443a03a50d9773d91350a8582

[MODIFIED] app/Filament/Resources/OrderResource.php
    why    : Adds the payment column/filter/section and the Verify payment, Reject payment and Add delivery admin actions.
    bytes  : 24330
    sha256 : 51100679beaba2f752a64a982f59b6178c99da55ab402127856f61b4593e6090

[MODIFIED] routes/api.php
    why    : *** CONTAINS BOTH CHANGES *** the earlier sitemap route AND the two new payment routes. See the note below.
    bytes  : 21019
    sha256 : b7a1f130e0d8f6212dabea880d465e4231d8485d9836552e28fbcf2c28d6ff57

[NEW] tests/Feature/Payments/InternationalPrepaymentTest.php
    why    : 20 tests proving the rules through HTTP. OPTIONAL on the server - not needed at runtime.
    bytes  : 16334
    sha256 : 2b81cd514bacdb08e1884778c1bd7a907803bc7cefdd8f79f3f9e84cc1994f2b

[MODIFIED] tests/Feature/SourcingTest.php
    why    : Existing tests updated because imports may no longer be paid cash on delivery. OPTIONAL on the server - not needed at runtime.
    bytes  : 26552
    sha256 : d614e49142d314fab49bd36786d9510966da28e59adf198bfd0b028a73f5ddbd

----------------------------------------------------------------------
DATABASE MIGRATIONS INCLUDED
----------------------------------------------------------------------

  1. 2026_08_23_000010_create_checkout_payment_channels_table.php
     CREATE TABLE checkout_payment_channels
       code, label, merchant_name, number, instructions,
       is_active, requires_reference, requires_verification, sort_order

  2. 2026_08_23_000020_add_payment_state_to_orders_table.php
     ALTER TABLE orders ADD:
       payment_status         (default 'not_required' - correct for every
                               existing row, which is all cash on delivery)
       payment_reference
       payment_submitted_at
       payment_verified_at
       payment_verified_by    (FK -> users, nullOnDelete)
       payment_note

  ORDERING: the timestamps 000010 then 000020 guarantee the table exists
  before the orders columns are added. Both are ADDITIVE - up() contains no
  DROP, no TRUNCATE, no DELETE. The only dropColumn is inside down().

----------------------------------------------------------------------
SEEDER INCLUDED
----------------------------------------------------------------------

  database/seeders/CheckoutPaymentChannelSeeder.php

  Safe to run more than once: it uses firstOrCreate() keyed on `code`, so a
  second run will NOT overwrite a number an administrator has already set.

  It creates both rows with:
      number    = NULL
      is_active = FALSE

  That is deliberate. Lipa Namba stays switched off until an administrator
  types the real number in the admin panel. No number - real or fake - is
  shipped in this package.

----------------------------------------------------------------------
FILES NEEDING SPECIAL ATTENTION
----------------------------------------------------------------------

  routes/api.php  -- INCLUDED, and it contains BOTH sets of changes:

      (B) the earlier SEO work, already live on your server:
            Route::get('/sitemap', [ShopCatalogController::class,'sitemap'])
                ->middleware('cacheable:1800,7200');

      (A) the new international prepayment routes:
            Route::get('/payment-channels', ...'channels');
            Route::post('/orders/{reference}/payment', ...'submit');

      The version in this package is the COMPLETE MERGED FILE. Uploading it
      keeps the sitemap route. Verified after packaging - see below.

  app/Http/Controllers/Api/Shop/CatalogController.php  -- DELIBERATELY EXCLUDED

      Checked against the repository: its only difference from the last
      commit is the sitemap() method (+71 lines, 0 deletions) and it contains
      ZERO references to payment, CheckoutPolicy or Lipa Namba.

      That code is already running on your server - https://api.2konect.shop
      /api/shop/sitemap returns HTTP 200 with live catalogue data. Since it
      carries no prepayment change, shipping it would risk overwriting the
      live copy for no benefit. It is therefore left alone.

  tests/  -- OPTIONAL

      Not required at runtime. Included because they are the proof the rules
      work. DO NOT run the test suite on the production server: the suite
      uses RefreshDatabase, and if the server's phpunit.xml does not pin
      sqlite/:memory: it would rebuild whatever database it does point at.
      Skip this folder entirely if you would rather not have it there.

----------------------------------------------------------------------
SITEMAP / SEO PRESERVATION - CONFIRMED
----------------------------------------------------------------------

  [x] routes/api.php in this package contains the /sitemap route
  [x] CatalogController.php (which defines sitemap()) is NOT overwritten
  [x] no file in this package removes or edits any SEO code
  [x] uploading this package cannot remove sitemap functionality

----------------------------------------------------------------------
WHAT IS NOT IN THIS PACKAGE
----------------------------------------------------------------------

  no vendor/         no node_modules/    no .git/        no .env
  no storage/logs    no caches           no databases    no IDE files
  no frontend files  no test users       no test orders  no credentials
  no Lipa Namba number of any kind in any runtime file

